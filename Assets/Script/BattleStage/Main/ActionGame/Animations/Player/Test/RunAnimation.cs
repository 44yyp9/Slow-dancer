using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UniRx;

public class RunAnimation : MonoBehaviour
{
    /*
    [SerializeField] private float runningSpeed;
    private void run()
    {
        Vector3 running=new Vector3 (runningSpeed*100,0f,0f);
        gameObject.transform.position += running * GameTime.playingTime;
    }
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            run();
        }
    }
    */
}
