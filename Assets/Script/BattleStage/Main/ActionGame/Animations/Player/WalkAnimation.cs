using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UniRx;

public class WalkAnimation : MonoBehaviour
{
    /*
    [SerializeField] private float walkingSpeed;
    private void walk()
    {
        Vector3 walking = new Vector3(walkingSpeed, 0f, 0f);
        gameObject.transform.position += walking * GameTime.playingTime;
    }
    private void Update()
    {
        if (Input.GetKey(KeyCode.RightArrow))
        {
            walk();
            Debug.Log("walk");
        }
    }
    */
}
