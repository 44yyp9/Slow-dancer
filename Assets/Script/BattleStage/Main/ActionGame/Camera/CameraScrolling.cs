using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UniRx;

public class CameraScrolling : MonoBehaviour
{
    /*
    public Transform playerPosi;
    [SerializeField] private float scrollingSpeed;
    [SerializeField] private float scrollingValue;
    public void scroll()
    {
        Vector3 scrolling = new Vector3(scrollingSpeed,0, 0);
        gameObject.transform.position += scrolling * GameTime.playingTime;
    }
    */
}
